server:
  port: 9001
spring:
  application:
    name: cloud-provider
  cloud:
    nacos:
      discovery:
        server-addr:  127.0.0.1:8848 #Nacos配置地址

#暴露监控端口
management:
  endpoints:
    web:
      exposure:
        include: "*"
